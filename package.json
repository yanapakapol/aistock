{
  "name": "aistock",
  "private": true,
  "version": "0.0.0",
  "workspaces": ["apps/*"],
  "scripts": {
    "dev": "npm run -w apps/web dev",
    "build": "npm run -w apps/web build",
    "start": "npm run -w apps/web start",
    "lint": "npm run -w apps/web lint",
    "db:generate": "npm run -w apps/web db:generate",
    "db:migrate": "npm run -w apps/web db:migrate",
    "seed-models": "tsx scripts/seed-models.ts"
  },
  "devDependencies": {
    "tsx": "^4.19.2"
  }
}
